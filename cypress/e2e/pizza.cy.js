describe("Pizza App", () => {
    beforeEach(() => {
        cy.visit("https://localhost:3003/");
    })

 it("sanity check to make sure tests work", () => {
    expect(1 + 2).to.equal(3);
 })

















})